# backend

Server side for spotter-everywhere. Intended role: receive the viz context and
data the extension collects, talk to ThoughtSpot (Spotter) on the user's behalf,
and return insights to the panel. Stack not chosen yet.
